/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/healthz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Liveness probe. */
        get: operations["getHealth"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/readyz": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Readiness probe. 503 if any configured cluster is unreachable. */
        get: operations["getReadiness"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/info": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Build info. */
        get: operations["getInfo"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/me": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** Resolved caller identity and effective permissions. */
        get: operations["getMe"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/openapi.yaml": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** This OpenAPI document. */
        get: operations["getOpenApiSpec"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /** List configured clusters with reachability, capabilities and metrics. */
        get: operations["listClusters"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/_test": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Probe an ad-hoc cluster definition without storing it.
         * @description Validates a user-supplied cluster definition, pings the brokers and
         *     runs a short capability probe. The definition is taken from the
         *     request body or, when the body is empty, from the `X-Kafkito-Cluster`
         *     header (the body wins). A failed ping is **not** an HTTP error: the
         *     response is 200 with `reachable: false` and the raw connection error
         *     in `error`, because diagnosing the caller's own broker is the point of
         *     this endpoint. The probe budget defaults to 15 s
         *     (`KAFKITO_TEST_CONNECTION_TIMEOUT`).
         */
        post: operations["testCluster"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/capabilities": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        /** Cached capabilities (permissions probed against the broker). */
        get: operations["getCapabilities"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/capabilities/refresh": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Re-probe capabilities (clears the cache). */
        post: operations["refreshCapabilities"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/brokers": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        /** List the brokers of a cluster. */
        get: operations["listBrokers"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        /** List topics (filtered by RBAC view permission). */
        get: operations["listTopics"];
        put?: never;
        /** Create a topic. */
        post: operations["createTopic"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        /** Describe a single topic (partitions, offsets, configs). */
        get: operations["describeTopic"];
        put?: never;
        post?: never;
        /** Delete a topic. */
        delete: operations["deleteTopic"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/consumers": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        /** Consumer groups that have committed offsets on, or are assigned to, this topic. */
        get: operations["listTopicConsumers"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/configs": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        /** Incrementally set and/or delete topic config entries. */
        patch: operations["alterTopicConfigs"];
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/records": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete records up to an offset per partition (low-watermark bump). */
        delete: operations["deleteRecords"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/messages": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        /**
         * Browse a bounded page of messages.
         * @description Reads at most `limit` messages (capped at 500 server-side). Values are
         *     decoded into `value` when printable; binary values are rendered as a
         *     hex preview with the raw bytes in `value_b64`. Schema-Registry encoded
         *     records are decoded transparently when a Schema Registry is configured
         *     for the cluster. Large values are truncated for the list view
         *     (`value_truncated`); fetch the full bytes via the raw endpoint.
         *
         *     Paging: pass the returned `next_cursor` back as `cursor`. A cursor
         *     forces the seek mode to its direction (backward → `from=end`, forward
         *     → `from=offset`); an explicit contradicting `from` returns 400.
         */
        get: operations["consumeMessages"];
        put?: never;
        /**
         * Produce a single record.
         * @description `key_encoding` / `value_encoding` select how `key` / `value` are turned
         *     into record bytes:
         *     - `text` (default): the string is sent as UTF-8 bytes. An **empty**
         *       `text` value produces a **nil** payload, i.e. a tombstone — which
         *       deletes the key on a compacted topic.
         *     - `base64`: the string is base64-decoded (standard, URL-safe or raw
         *       standard are all accepted), for arbitrary binary payloads. An empty
         *       string produces a nil payload here too.
         *     - `empty`: the string is ignored and a non-nil **zero-length** payload
         *       is produced. Needed because `text` cannot express a zero-length
         *       payload (see the tombstone semantics above).
         *
         *     Header values that are not valid UTF-8 cannot round-trip through
         *     `headers`; pass them as base64 raw bytes in `headers_b64` instead. A key
         *     present in both maps wins in `headers_b64` and is emitted exactly once.
         *     An undecodable `headers_b64` value fails the request with 400.
         *
         *     kafkito always injects the provenance headers `X-Kafkito-Source: true`
         *     and, when an identity is available, `X-Kafkito-User: <subject>`,
         *     overwriting those keys if the request supplied them.
         *
         *     The body may be sent gzip-compressed with `Content-Encoding: gzip`.
         *     The decompressed body is capped at 15 MiB either way; unknown JSON
         *     fields are rejected.
         */
        post: operations["produceMessage"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/messages/count": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        /**
         * Approximate message volume for a topic range.
         * @description Resolves the selected timestamp window to per-partition offset deltas
         *     and returns an approximate count without consuming any records.
         */
        get: operations["countMessages"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/messages/timeline": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        /**
         * Time-sliced message volume over a time range for a topic.
         * @description Resolves the selected timestamp window into fixed-width time slots and
         *     returns an approximate message count per slot, without consuming
         *     any records. At most 400 slots per request.
         */
        get: operations["getMessageTimeline"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/messages/{partition}/{offset}/raw": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
                partition: number;
                offset: number;
            };
            cookie?: never;
        };
        /**
         * Download the raw value bytes of a single record.
         * @description Streams the untouched value bytes as an attachment
         *     (`<topic>-p<partition>-o<offset>.<ext>`). The content type is sniffed:
         *     `application/json` for valid JSON, `text/plain` for other UTF-8 and
         *     `application/octet-stream` otherwise. Values above 15 MB are rejected
         *     with 413.
         */
        get: operations["downloadMessageRaw"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/sample": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        /**
         * Sample the last n decoded messages from a topic.
         * @description Returns up to `n` most recent decoded messages. Used by the search
         *     path-picker to build a structural sample of the topic payload. Reuses
         *     the consume permission for RBAC.
         */
        get: operations["sampleMessages"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/messages/search": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /** Bounded, budget-limited content search across partitions. */
        post: operations["searchMessages"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/topics/{topic}/copy": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Bulk-copy messages into another topic (SSE progress stream).
         * @description Server-side copy: reads records from the source topic — the
         *     `{cluster}`/`{topic}` in the URL — and re-produces them into
         *     `dest_topic` on the destination cluster named in the request body.
         *     The destination topic must already exist; kafkito never creates it.
         *
         *     Exactly one destination cluster field must be set: `dest_cluster` for a
         *     server-configured cluster, or `dest_cluster_config` for an ad-hoc
         *     ("private") cluster passed inline — the same payload the
         *     `X-Kafkito-Cluster` header carries.
         *
         *     The response is **not** JSON: it is a `text/event-stream` of
         *     `CopyProgressEvent` objects, because a copy can run far longer than a
         *     normal request.
         *
         *     When `to_ts_ms` is omitted the server substitutes the job's start time,
         *     so copying a live topic terminates instead of tailing it forever:
         *     records produced after the copy started are not included.
         *
         *     Records that cannot be reproduced byte-for-byte are left out and
         *     counted in `skipped` instead of being copied approximately:
         *     Schema-Registry-decoded payloads (`avro` / `json_schema` /
         *     `protobuf`), for which only the decoded JSON rendering is available and
         *     the original wire-format bytes are gone, and masked records, whose
         *     value the source cluster's `data_masking` rules replaced with a
         *     redacted rendering.
         *
         *     Copied records carry the same provenance headers the produce endpoint
         *     injects — `X-Kafkito-Source: true` and `X-Kafkito-User: <subject>` —
         *     overwriting those keys if the source record already had them.
         *
         *     Authorization: the source needs `topic:consume` (checked by the RBAC
         *     middleware from the URL) and the destination needs `topic:produce`
         *     (checked by the handler against the cluster/topic named in the body).
         *     An ad-hoc `dest_cluster_config` destination **bypasses RBAC** — the
         *     caller supplies their own broker credentials and only that broker's own
         *     ACLs apply, so a user holding nothing but `topic:consume` can stream a
         *     readable topic to a broker of their choosing. This is a deliberate,
         *     pre-existing property of private clusters.
         *
         *     The copy is neither transactional nor resumable: on error the records
         *     copied so far stay in the destination topic, and re-running the copy
         *     duplicates them.
         */
        post: operations["copyMessages"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/groups": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        /** List consumer groups (filtered by RBAC view permission). */
        get: operations["listGroups"];
        put?: never;
        /**
         * Create a consumer group bound to a topic.
         * @description Creates a new consumer group by committing initial offsets for all
         *     partitions of `topic` under the chosen strategy. Fails with 409 if the
         *     group already exists. `shift-by` is not accepted. Set `dry_run=true` to
         *     preview the resulting offsets without creating the group.
         */
        post: operations["createGroup"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/groups/{group}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                group: components["parameters"]["Group"];
            };
            cookie?: never;
        };
        /**
         * Describe a consumer group (members, offsets, lag).
         * @description Returns full membership and per-partition committed offsets.
         *     `offsets[].assigned_to` is `<client_id>@<host>` of the current partition
         *     owner, or absent if the group is rebalancing.
         */
        get: operations["describeGroup"];
        put?: never;
        post?: never;
        /** Delete a consumer group. Must be empty. */
        delete: operations["deleteGroup"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/groups/{group}/reset-offsets": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                group: components["parameters"]["Group"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Reset committed offsets of a group.
         * @description Group must have no active members. Set `dry_run=true` to preview the
         *     resulting offsets without committing.
         */
        post: operations["resetGroupOffsets"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/schemas/subjects": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        /** List Schema Registry subjects with their versions. */
        get: operations["listSubjects"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/schemas/subjects/{subject}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                subject: components["parameters"]["Subject"];
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete a subject (soft by default). */
        delete: operations["deleteSubject"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/schemas/subjects/{subject}/versions": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                subject: components["parameters"]["Subject"];
            };
            cookie?: never;
        };
        /** List versions of a subject. */
        get: operations["listSchemaVersions"];
        put?: never;
        /** Register a new version for a subject. */
        post: operations["registerSchema"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/schemas/subjects/{subject}/versions/{version}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                subject: components["parameters"]["Subject"];
                /** @description Integer version or `latest`. */
                version: string;
            };
            cookie?: never;
        };
        /** Get a specific subject version (or `latest`). */
        get: operations["getSchemaVersion"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/acls": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        /** List ACLs. */
        get: operations["listAcls"];
        put?: never;
        /** Create an ACL. */
        post: operations["createAcl"];
        /** Delete ACL(s) matching a filter. */
        delete: operations["deleteAcl"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/users": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        /** List SCRAM users. */
        get: operations["listScramUsers"];
        put?: never;
        /** Create or update a SCRAM user. */
        post: operations["upsertScramUser"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/api/v1/clusters/{cluster}/users/{user}": {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                user: string;
            };
            cookie?: never;
        };
        get?: never;
        put?: never;
        post?: never;
        /** Delete a SCRAM user's credentials. */
        delete: operations["deleteScramUser"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /** @description Error body emitted by all JSON error paths. */
        Error: {
            /** @description Human-readable message (`unauthorized`, `forbidden`, `not found`, ...). */
            error: string;
            /** @description Machine-readable code where one exists, e.g. `kafka_upstream`, `production_confirmation_required`, `copy_concurrency_limit`, `kafka_message_too_large`, `kafka_not_authorized`, `rbac_denied`, `topic_consumers_timeout`. */
            code?: string;
            /** @description Detail for 401 responses from the auth middleware. */
            message?: string;
            /** @description RBAC-denied resource as `<type>:<name>` (403 only). */
            resource?: string;
            /** @description RBAC-denied action (403 only). */
            action?: string;
        };
        HealthResponse: {
            /** @constant */
            status: "ok";
        };
        ReadinessResponse: {
            /** @enum {string} */
            status: "ok" | "ready" | "degraded";
            clusters: components["schemas"]["ClusterInfo"][];
            /** @description Set when no clusters are configured. */
            note?: string;
        };
        InfoResponse: {
            name: string;
            version: string;
        };
        MeResponse: {
            /** @description Resolved RBAC subject; empty when anonymous. */
            user: string;
            email: string;
            tenant: string;
            scopes: string[] | null;
            roles: string[] | null;
            /** @description Map of resource type (or `*`) to allowed actions. */
            permissions: {
                [key: string]: string[];
            };
            anonymous: boolean;
            /** @description True when a validated principal is present. */
            jwt: boolean;
            rbac_enabled: boolean;
        };
        ListClustersResponse: {
            clusters: components["schemas"]["ClusterInfo"][];
        };
        ClusterInfo: {
            name: string;
            reachable: boolean;
            error?: string;
            is_prod: boolean;
            auth_type: string;
            tls: boolean;
            schema_registry: boolean;
            capabilities?: components["schemas"]["Capabilities"];
            /** @description Aggregate metrics below are best-effort and absent until known. */
            brokers?: number;
            topics?: number;
            groups?: number;
            /** Format: int64 */
            total_messages?: number;
            /** Format: int64 */
            total_lag?: number;
            /** Format: double */
            total_rate_per_sec?: number;
        };
        Capabilities: {
            describe_cluster: boolean;
            list_topics: boolean;
            describe_configs: boolean;
            list_groups: boolean;
            create_topic: boolean;
            delete_topic: boolean;
            alter_configs: boolean;
            errors?: {
                [key: string]: string;
            };
            /** Format: date-time */
            probed_at: string;
        };
        CapabilitiesResponse: {
            cluster: string;
            capabilities: components["schemas"]["Capabilities"];
        };
        BrokerInfo: {
            /** Format: int32 */
            node_id: number;
            host: string;
            /** Format: int32 */
            port: number;
            rack?: string;
            is_controller: boolean;
        };
        ListBrokersResponse: {
            cluster: string;
            brokers: components["schemas"]["BrokerInfo"][];
        };
        TopicInfo: {
            name: string;
            partitions: number;
            replication_factor: number;
            is_internal: boolean;
            /**
             * Format: int64
             * @description Metric fields are best-effort and absent until known.
             */
            messages?: number;
            /** Format: int64 */
            size_bytes?: number;
            /**
             * Format: int64
             * @description -1 means infinite.
             */
            retention_ms?: number;
            /** Format: double */
            rate_per_sec?: number;
            /** Format: int64 */
            lag?: number;
        };
        ListTopicsResponse: {
            cluster: string;
            topics: components["schemas"]["TopicInfo"][];
        };
        CreateTopicRequest: {
            name: string;
            /**
             * Format: int32
             * @description 0 or -1 = broker default.
             */
            partitions?: number;
            /**
             * Format: int32
             * @description 0 or -1 = broker default.
             */
            replication_factor?: number;
            configs?: {
                [key: string]: string;
            };
        };
        CreatedResponse: {
            created: string;
        };
        DeletedNameResponse: {
            deleted: string;
        };
        DescribeTopicResponse: {
            cluster: string;
            topic: components["schemas"]["TopicDetail"];
        };
        TopicDetail: {
            name: string;
            is_internal: boolean;
            partitions: components["schemas"]["PartitionInfo"][];
            replication_factor: number;
            /** Format: int64 */
            messages: number;
            configs: components["schemas"]["TopicConfigEntry"][];
            /** @description Non-empty when DescribeConfigs failed for this topic and the `configs` array is incomplete. Known codes: `unauthorized` (missing DescribeConfigs ACL on the topic / cluster), `unavailable` (any other broker-side error). */
            configs_error?: string;
            /** Format: int64 */
            size_bytes?: number;
        };
        PartitionInfo: {
            /** Format: int32 */
            partition: number;
            /** Format: int32 */
            leader: number;
            replicas: number[];
            isr: number[];
            /** Format: int64 */
            start_offset: number;
            /** Format: int64 */
            end_offset: number;
            /** Format: int64 */
            messages: number;
        };
        TopicConfigEntry: {
            name: string;
            value: string;
            is_default: boolean;
            source?: string;
            sensitive: boolean;
        };
        AlterTopicConfigsRequest: {
            set?: {
                [key: string]: string;
            };
            delete?: string[];
        };
        AlterTopicConfigsResult: {
            name: string;
            /** @enum {string} */
            op: "set" | "delete";
            value?: string;
            error?: string;
        };
        AlterTopicConfigsResponse: {
            results: components["schemas"]["AlterTopicConfigsResult"][];
        };
        TopicConsumer: {
            group_id: string;
            state: string;
            members: number;
            partitions_assigned: number[];
            /** Format: int64 */
            lag: number;
            lag_known: boolean;
            error?: string;
        };
        ListTopicConsumersResponse: {
            cluster: string;
            topic: string;
            consumers: components["schemas"]["TopicConsumer"][];
        };
        DeleteRecordsRequest: {
            /** @description Map of partition id → offset to delete up to (-1 = high watermark). */
            partitions: {
                [key: string]: number;
            };
        };
        DeleteRecordsResult: {
            /** Format: int32 */
            partition: number;
            /** Format: int64 */
            low_watermark: number;
            /** Format: int64 */
            requested_offset: number;
            error?: string;
        };
        DeleteRecordsResponse: {
            results: components["schemas"]["DeleteRecordsResult"][];
        };
        SRDecodedMeta: {
            /** @description `avro`, `protobuf` or `json_schema`. */
            format?: string;
            schema_id?: number;
            subject?: string;
            version?: number;
        };
        Message: {
            /** Format: int32 */
            partition: number;
            /** Format: int64 */
            offset: number;
            /** Format: int64 */
            timestamp_ms: number;
            key?: string;
            /** @description How `key` is rendered: `null`, `empty`, `json`, `text`, `binary` (hex preview, raw bytes in `key_b64`) or a Schema Registry format (`avro`, `protobuf`, `json_schema`). */
            key_encoding: string;
            key_b64?: string;
            value?: string;
            /** @description Same values as `key_encoding`. */
            value_encoding: string;
            value_b64?: string;
            /** @description Header values as text. A value that is not valid UTF-8 is rendered as `0x…` hex here for display only — its raw bytes are in `headers_b64`. */
            headers?: {
                [key: string]: string;
            };
            /** @description Standard-base64 raw bytes of header values that are not valid UTF-8. Only the affected keys appear; they also remain in `headers` with the hex rendering. Use this map to reproduce a header byte-for-byte (see the produce endpoint's `headers_b64`). */
            headers_b64?: {
                [key: string]: string;
            };
            masked?: boolean;
            /**
             * Format: int64
             * @description Untruncated byte length of the raw value; absent for nil/empty values.
             */
            value_size_bytes?: number;
            /** @description True when `value` holds only a preview; use the raw endpoint for the full bytes. */
            value_truncated?: boolean;
            key_sr?: components["schemas"]["SRDecodedMeta"];
            value_sr?: components["schemas"]["SRDecodedMeta"];
        };
        MessagesPage: {
            cluster: string;
            topic: string;
            messages: components["schemas"]["Message"][];
            has_more: boolean;
            /** @description Opaque cursor for the next page. */
            next_cursor?: string;
            /** @description True when a `from=end` page could not fully collect its tail window before the server-side timeout; the newest record(s) may be missing. */
            partial?: boolean;
        };
        ProduceRequest: {
            /**
             * Format: int32
             * @description Explicit partition. Absent = key-based partitioning.
             */
            partition?: number;
            key?: string;
            value?: string;
            /**
             * @default text
             * @enum {string}
             */
            key_encoding?: "text" | "base64" | "empty";
            /**
             * @default text
             * @enum {string}
             */
            value_encoding?: "text" | "base64" | "empty";
            /** @description Header values as UTF-8 text. */
            headers?: {
                [key: string]: string;
            };
            /** @description Header values as standard-base64 raw bytes, for headers whose value is not valid UTF-8. Wins over `headers` for the same key. */
            headers_b64?: {
                [key: string]: string;
            };
        };
        ProduceResult: {
            topic: string;
            /** Format: int32 */
            partition: number;
            /** Format: int64 */
            offset: number;
            /** Format: int64 */
            timestamp_ms: number;
        };
        MessageCountPartition: {
            /** Format: int32 */
            partition: number;
            /** Format: int64 */
            from_offset: number;
            /** Format: int64 */
            to_offset: number;
            /** Format: int64 */
            approx_count: number;
        };
        MessageCountResponse: {
            cluster: string;
            topic: string;
            /** Format: int64 */
            from_ts_ms?: number;
            /** Format: int64 */
            to_ts_ms?: number;
            /** Format: int64 */
            total_approx_count: number;
            partitions: components["schemas"]["MessageCountPartition"][];
        };
        MessageTimelineSlot: {
            /** Format: int64 */
            from_ts_ms: number;
            /** Format: int64 */
            to_ts_ms: number;
            /** Format: int64 */
            approx_count: number;
        };
        MessageTimelineResponse: {
            cluster: string;
            topic: string;
            /** Format: int64 */
            from_ts_ms: number;
            /** Format: int64 */
            to_ts_ms: number;
            /** Format: int64 */
            slot_ms: number;
            slots: components["schemas"]["MessageTimelineSlot"][];
        };
        SampleResponse: {
            cluster: string;
            topic: string;
            messages: components["schemas"]["Message"][];
            /** Format: int64 */
            sampled_at: number;
        };
        /** @description All fields are optional; server-side defaults apply. */
        SearchRequest: {
            /**
             * Format: int32
             * @description Absent or -1 = all partitions.
             */
            partition?: number;
            /** @description Maximum matches to return. */
            limit?: number;
            /** @description Maximum records to scan. */
            budget?: number;
            /** @enum {string} */
            direction?: "newest_first" | "oldest_first";
            /** @default true */
            stop_on_limit?: boolean;
            /** @enum {string} */
            mode?: "contains" | "jsonpath" | "xpath" | "js";
            /** @description JSONPath / XPath expression or JS filter source. */
            path?: string;
            /** @enum {string} */
            op?: "exists" | "eq" | "ne" | "contains" | "regex" | "gt" | "lt" | "gte" | "lte";
            value?: string;
            zones?: ("value" | "key" | "headers")[];
            /** Format: int64 */
            from_ts_ms?: number;
            /** Format: int64 */
            to_ts_ms?: number;
            /** @description Per-partition continuation offsets from `next_cursors`, keyed by partition id. */
            cursors?: {
                [key: string]: number;
            };
        };
        PartitionRange: {
            /** Format: int64 */
            start: number;
            /** Format: int64 */
            end: number;
        };
        ParseErrorOffset: {
            /** Format: int32 */
            partition: number;
            /** Format: int64 */
            offset: number;
            error: string;
        };
        SearchStats: {
            scanned: number;
            matched: number;
            budget_exhausted: boolean;
            timed_out: boolean;
            more_available: boolean;
            /** @enum {string} */
            direction: "newest_first" | "oldest_first";
            next_cursors?: {
                [key: string]: number;
            };
            resolved_range?: {
                [key: string]: components["schemas"]["PartitionRange"];
            };
            parse_errors: number;
            parse_error_offsets?: components["schemas"]["ParseErrorOffset"][];
            durations_ms?: {
                [key: string]: number;
            };
        };
        SearchResponse: {
            cluster: string;
            topic: string;
            messages: components["schemas"]["Message"][] | null;
            search: components["schemas"]["SearchStats"];
        };
        /** @description Body of the topic copy endpoint. Exactly one of `dest_cluster` / `dest_cluster_config` must be set. */
        CopyRequest: {
            /** @description Name of a server-configured destination cluster. Mutually exclusive with `dest_cluster_config`. */
            dest_cluster?: string;
            /** @description Ad-hoc ("private") destination cluster, same shape the `X-Kafkito-Cluster` header carries. Mutually exclusive with `dest_cluster`. Bypasses RBAC on the destination. */
            dest_cluster_config?: components["schemas"]["ClusterConfig"];
            /** @description Destination topic. Must already exist. */
            dest_topic: string;
            /**
             * Format: int32
             * @description Single source partition. Absent = all partitions.
             */
            partition?: number;
            /**
             * Format: int64
             * @description Inclusive lower bound on source record timestamps.
             */
            from_ts_ms?: number;
            /**
             * Format: int64
             * @description Exclusive upper bound on source record timestamps. When omitted the server substitutes the job's start time, so a copy of a live topic terminates instead of tailing it forever.
             */
            to_ts_ms?: number;
            /**
             * Format: int64
             * @description Maximum number of records to copy. Absent = no limit.
             */
            limit?: number;
            /** @description Produce each record to the same partition number it came from. Requires the destination topic to have at least as many partitions as the highest source partition, otherwise the request is rejected with 400. */
            preserve_partition?: boolean;
        };
        /** @description One SSE event of the copy stream, sent as a `data: {json}` line pair. Progress events arrive periodically — one right after the stream opens and at least one per fetched page — and the final event has `done: true`. */
        CopyProgressEvent: {
            /**
             * Format: int64
             * @description Records produced to the destination topic so far.
             */
            copied: number;
            /**
             * Format: int64
             * @description Source records deliberately left out because they cannot be reproduced byte-for-byte (Schema-Registry-decoded or masked values). Omitted while 0.
             */
            skipped?: number;
            /** @description True on the final event only; omitted otherwise. */
            done?: boolean;
            /** @description Set on the final event when the job aborted. Omitted when empty. Errors after the stream opened still carry HTTP status 200. */
            error?: string;
        };
        /** @description Ad-hoc ("private") cluster definition supplied per request instead of from the server config — the same JSON the `X-Kafkito-Cluster` header carries (base64-encoded there). Broker and Schema Registry hosts are subject to the server's outbound-host (SSRF) policy. */
        ClusterConfig: {
            name?: string;
            is_prod?: boolean;
            brokers: string[];
            auth?: {
                /**
                 * @description Case-insensitive. `plain` / `scram-*` require `username` and `password`.
                 * @enum {string}
                 */
                type?: "" | "none" | "plain" | "scram-sha-256" | "scram-sha-512";
                username?: string;
                /** Format: password */
                password?: string;
            };
            tls?: {
                enabled?: boolean;
                insecure_skip_verify?: boolean;
            };
            schema_registry?: {
                url?: string;
                username?: string;
                /** Format: password */
                password?: string;
                insecure_skip_verify?: boolean;
            };
            data_masking?: components["schemas"]["MaskingRule"][] | null;
        };
        MaskingRule: {
            topics?: string[];
            /** @description JSONPath expressions whose values are replaced. */
            fields?: string[];
            regex?: {
                match?: string;
                replacement?: string;
            }[];
            /** @description Replacement for `fields`; empty = "***". */
            replacement?: string;
        };
        GroupInfo: {
            group_id: string;
            /** @description Broker-reported state, e.g. Stable, PreparingRebalance, CompletingRebalance, Empty, Dead. */
            state: string;
            protocol_type: string;
            /** @description e.g. range, roundrobin, cooperative-sticky. */
            protocol?: string;
            /** Format: int32 */
            coordinator_id: number;
            /** @description Member count. */
            members: number;
            topics?: number;
            /** Format: int64 */
            lag: number;
            lag_known: boolean;
            error?: string;
        };
        ListGroupsResponse: {
            cluster: string;
            groups: components["schemas"]["GroupInfo"][];
        };
        MemberAssignment: {
            topic: string;
            partitions: number[];
        };
        GroupMember: {
            member_id: string;
            /** @description Static-membership group.instance.id, if configured. */
            instance_id?: string;
            client_id: string;
            client_host: string;
            assignments: components["schemas"]["MemberAssignment"][];
        };
        GroupOffset: {
            topic: string;
            /** Format: int32 */
            partition: number;
            /** Format: int64 */
            offset: number;
            /** Format: int64 */
            log_end: number;
            /** Format: int64 */
            lag: number;
            metadata?: string;
            /** @description Current owner as `<client_id>@<host>`, absent during rebalance. */
            assigned_to?: string;
        };
        /** @description GroupInfo fields plus membership and offsets. `members` is the member list here, not the count from GroupInfo. */
        GroupDetail: {
            group_id: string;
            state: string;
            protocol_type: string;
            protocol?: string;
            /** Format: int32 */
            coordinator_id: number;
            topics?: number;
            /** Format: int64 */
            lag: number;
            lag_known: boolean;
            error?: string;
            members: components["schemas"]["GroupMember"][];
            offsets: components["schemas"]["GroupOffset"][];
        };
        CreateGroupRequest: {
            group_id: string;
            topic: string;
            /** @enum {string} */
            strategy: "earliest" | "latest" | "offset" | "timestamp";
            /**
             * Format: int64
             * @description Used with strategy=offset.
             */
            offset?: number;
            /**
             * Format: int64
             * @description Used with strategy=timestamp.
             */
            timestamp_ms?: number;
            dry_run?: boolean;
        };
        ResetOffsetsRequest: {
            topic: string;
            /** @description Absent or empty = all partitions of `topic`. */
            partitions?: number[];
            /** @enum {string} */
            strategy: "earliest" | "latest" | "offset" | "timestamp" | "shift-by";
            /**
             * Format: int64
             * @description Used with strategy=offset.
             */
            offset?: number;
            /**
             * Format: int64
             * @description Used with strategy=timestamp.
             */
            timestamp_ms?: number;
            /**
             * Format: int64
             * @description Used with strategy=shift-by.
             */
            shift?: number;
            dry_run?: boolean;
        };
        ResetOffsetResult: {
            /** Format: int32 */
            partition: number;
            /**
             * Format: int64
             * @description -1 if no prior commit.
             */
            old_offset: number;
            /** Format: int64 */
            new_offset: number;
            /**
             * Format: int64
             * @description Log-end offset, -1 if unknown.
             */
            end_offset: number;
            error?: string;
        };
        ResetOffsetsResponse: {
            group: string;
            topic: string;
            dry_run: boolean;
            results: components["schemas"]["ResetOffsetResult"][];
        };
        Subject: {
            name: string;
            versions: number[];
            latest_schema_type?: string;
        };
        ListSubjectsResponse: {
            cluster: string;
            subjects: components["schemas"]["Subject"][];
        };
        ListSchemaVersionsResponse: {
            subject: string;
            versions: number[];
        };
        SchemaReference: {
            name: string;
            subject: string;
            version: number;
        };
        SchemaVersion: {
            subject: string;
            id: number;
            version: number;
            /** @description AVRO, JSON or PROTOBUF (may be empty for AVRO). */
            schemaType: string;
            schema: string;
            references?: components["schemas"]["SchemaReference"][];
            config?: {
                compatibilityLevel?: string;
            };
        };
        RegisterSchemaRequest: {
            schema: string;
            /**
             * @description Defaults to AVRO.
             * @enum {string}
             */
            schemaType?: "AVRO" | "JSON" | "PROTOBUF";
            references?: components["schemas"]["SchemaReference"][];
        };
        RegisterSchemaResponse: {
            id: number;
        };
        DeleteSubjectResponse: {
            deleted: string;
            versions: number[];
            permanent: boolean;
        };
        /** @description ACL binding. As a request body it is a create spec or a delete filter. Enum-like fields are parsed case-insensitively by franz-go (`kmsg`). */
        ACLEntry: {
            /** @example User:alice */
            principal: string;
            /** @example * */
            host: string;
            /**
             * @description e.g. TOPIC, GROUP, CLUSTER, TRANSACTIONAL_ID, DELEGATION_TOKEN (ANY for filters).
             * @example TOPIC
             */
            resource_type: string;
            resource_name: string;
            /**
             * @description e.g. LITERAL, PREFIXED (ANY / MATCH for filters).
             * @example LITERAL
             */
            pattern_type: string;
            /**
             * @description e.g. READ, WRITE, CREATE, DELETE, ALTER, DESCRIBE, CLUSTER_ACTION, ALTER_CONFIGS, DESCRIBE_CONFIGS, IDEMPOTENT_WRITE, ALL.
             * @example READ
             */
            operation: string;
            /**
             * @description ALLOW or DENY (ANY for filters).
             * @example ALLOW
             */
            permission_type: string;
        };
        ListACLsResponse: {
            cluster: string;
            acls: components["schemas"]["ACLEntry"][];
        };
        CreateACLResponse: {
            ok: boolean;
            acl: components["schemas"]["ACLEntry"];
        };
        DeleteACLResponse: {
            ok: boolean;
            /** @description Number of deleted ACL bindings. */
            deleted: number;
        };
        SCRAMCredential: {
            mechanism: string;
            /** Format: int32 */
            iterations: number;
        };
        SCRAMUser: {
            user: string;
            credentials: components["schemas"]["SCRAMCredential"][];
        };
        ListSCRAMUsersResponse: {
            cluster: string;
            users: components["schemas"]["SCRAMUser"][];
        };
        UpsertSCRAMUserRequest: {
            user: string;
            /** @enum {string} */
            mechanism: "SCRAM-SHA-256" | "SCRAM-SHA-512";
            /** Format: password */
            password: string;
            /**
             * Format: int32
             * @description 0 = server default.
             */
            iterations?: number;
        };
        UpsertSCRAMUserResponse: {
            ok: boolean;
            user: string;
            mechanism: string;
        };
        DeleteSCRAMUserResponse: {
            ok: boolean;
            user: string;
            /** @description Number of mechanisms deleted. */
            deleted: number;
        };
    };
    responses: {
        /** @description Invalid request (parameters, body, or private-cluster header). */
        BadRequest: {
            headers: {
                "X-Request-Id": components["headers"]["RequestId"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description Missing or invalid bearer token. */
        Unauthorized: {
            headers: {
                "X-Request-Id": components["headers"]["RequestId"];
                /** @description Always `Bearer realm="kafkito"`. */
                "WWW-Authenticate"?: string;
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description RBAC denied the action (`error: forbidden` plus `resource` and `action`, or `code: rbac_denied`), or the broker rejected the credential's ACLs (`code: kafka_not_authorized`). */
        Forbidden: {
            headers: {
                "X-Request-Id": components["headers"]["RequestId"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description Unknown cluster, topic, or Schema Registry not configured. */
        NotFound: {
            headers: {
                "X-Request-Id": components["headers"]["RequestId"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description Request body or record value exceeds the server-side limit. */
        PayloadTooLarge: {
            headers: {
                "X-Request-Id": components["headers"]["RequestId"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description Cluster is marked `is_prod` and the `X-Kafkito-Confirm-Prod: true` header is missing (`code: production_confirmation_required`). */
        ProdConfirmationRequired: {
            headers: {
                "X-Request-Id": components["headers"]["RequestId"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
        /** @description Upstream Kafka / Schema Registry error. Details are logged server-side only (`code: kafka_upstream`). */
        BadGateway: {
            headers: {
                "X-Request-Id": components["headers"]["RequestId"];
                [name: string]: unknown;
            };
            content: {
                "application/json": components["schemas"]["Error"];
            };
        };
    };
    parameters: {
        /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
        Cluster: string;
        Topic: string;
        Group: string;
        Subject: string;
        /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
        PrivateClusterHeader: string;
        /** @description Must be `true` when the target cluster is marked `is_prod`; the request is rejected with 428 otherwise. Send only after explicit user confirmation. */
        ProdConfirmHeader: "true";
        /** @description Partition number; -1 = all partitions. */
        PartitionQuery: number;
        /** @description Optional inclusive lower timestamp bound in Unix milliseconds. */
        FromTsMsQuery: number;
        /** @description Optional exclusive upper timestamp bound in Unix milliseconds. */
        ToTsMsQuery: number;
    };
    requestBodies: never;
    headers: {
        /** @description Request correlation id, set on every response. Reuses the inbound `X-Vcap-Request-Id`, the trace-id of a W3C `traceparent` or an inbound `X-Request-Id` when present (in that order), otherwise freshly generated. Matches the `request_id` field in the server logs. */
        RequestId: string;
    };
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    getHealth: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Service is alive. */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["HealthResponse"];
                };
            };
        };
    };
    getReadiness: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description All clusters reachable, or no clusters configured. */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReadinessResponse"];
                };
            };
            /** @description At least one configured cluster is unreachable. */
            503: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ReadinessResponse"];
                };
            };
        };
    };
    getInfo: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["InfoResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    getMe: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MeResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    getOpenApiSpec: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The raw OpenAPI 3.1 document. */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/yaml": string;
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    listClusters: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListClustersResponse"];
                };
            };
            401: components["responses"]["Unauthorized"];
        };
    };
    testCluster: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path?: never;
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["ClusterConfig"];
            };
        };
        responses: {
            /** @description Probe finished (see `reachable`). */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ClusterInfo"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
        };
    };
    getCapabilities: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CapabilitiesResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    refreshCapabilities: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CapabilitiesResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    listBrokers: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListBrokersResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    listTopics: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListTopicsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    createTopic: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateTopicRequest"];
            };
        };
        responses: {
            /** @description Created */
            201: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CreatedResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    describeTopic: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DescribeTopicResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    deleteTopic: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
                /** @description Must be `true` when the target cluster is marked `is_prod`; the request is rejected with 428 otherwise. Send only after explicit user confirmation. */
                "X-Kafkito-Confirm-Prod"?: components["parameters"]["ProdConfirmHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeletedNameResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            428: components["responses"]["ProdConfirmationRequired"];
            502: components["responses"]["BadGateway"];
        };
    };
    listTopicConsumers: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListTopicConsumersResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
            /** @description Listing consumers timed out (`code: topic_consumers_timeout`). */
            504: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
        };
    };
    alterTopicConfigs: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["AlterTopicConfigsRequest"];
            };
        };
        responses: {
            /** @description Per-entry results. */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AlterTopicConfigsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    deleteRecords: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
                /** @description Must be `true` when the target cluster is marked `is_prod`; the request is rejected with 428 otherwise. Send only after explicit user confirmation. */
                "X-Kafkito-Confirm-Prod"?: components["parameters"]["ProdConfirmHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["DeleteRecordsRequest"];
            };
        };
        responses: {
            /** @description Per-partition results. */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteRecordsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            428: components["responses"]["ProdConfirmationRequired"];
            502: components["responses"]["BadGateway"];
        };
    };
    consumeMessages: {
        parameters: {
            query?: {
                /** @description Partition number; -1 = all partitions. */
                partition?: components["parameters"]["PartitionQuery"];
                /** @description Page size. Values above 500 are clamped to 500. */
                limit?: number;
                /** @description Seek mode. `end` = newest records, `start` = oldest, `offset` = from `offset` (single partition) or `partition_offsets`, `timestamp` = first record at or after `from_ts_ms`. */
                from?: "end" | "start" | "offset" | "timestamp";
                /** @description Required when `from=offset` and `partition_offsets` is not set. */
                offset?: number;
                /** @description Per-partition seek offsets as `p:o,p:o` (max 1024 entries). Only valid with `from=offset`. */
                partition_offsets?: string;
                /** @description Optional inclusive lower timestamp bound in Unix milliseconds. */
                from_ts_ms?: components["parameters"]["FromTsMsQuery"];
                /** @description Optional exclusive upper timestamp bound in Unix milliseconds. */
                to_ts_ms?: components["parameters"]["ToTsMsQuery"];
                /** @description Opaque continuation token from a previous `next_cursor`. */
                cursor?: string;
            };
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MessagesPage"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    produceMessage: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
                /** @description Must be `true` when the target cluster is marked `is_prod`; the request is rejected with 428 otherwise. Send only after explicit user confirmation. */
                "X-Kafkito-Confirm-Prod"?: components["parameters"]["ProdConfirmHeader"];
                /** @description Set to `gzip` when the JSON body is gzip-compressed. */
                "Content-Encoding"?: "gzip";
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ProduceRequest"];
            };
        };
        responses: {
            /** @description Produced */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ProduceResult"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
            428: components["responses"]["ProdConfirmationRequired"];
            502: components["responses"]["BadGateway"];
        };
    };
    countMessages: {
        parameters: {
            query?: {
                /** @description Partition number; -1 = all partitions. */
                partition?: components["parameters"]["PartitionQuery"];
                /** @description Optional inclusive lower timestamp bound in Unix milliseconds. */
                from_ts_ms?: components["parameters"]["FromTsMsQuery"];
                /** @description Optional exclusive upper timestamp bound in Unix milliseconds. */
                to_ts_ms?: components["parameters"]["ToTsMsQuery"];
            };
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MessageCountResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    getMessageTimeline: {
        parameters: {
            query: {
                /** @description Partition number; -1 = all partitions. */
                partition?: components["parameters"]["PartitionQuery"];
                /** @description Inclusive lower timestamp bound in Unix milliseconds. */
                from_ts_ms: number;
                /** @description Exclusive upper timestamp bound in Unix milliseconds. Must be greater than `from_ts_ms`. */
                to_ts_ms: number;
                /** @description Time-slot width in milliseconds (e.g. 3600000 for hourly, 86400000 for daily). */
                slot_ms: number;
            };
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["MessageTimelineResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    downloadMessageRaw: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
                partition: number;
                offset: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Raw value bytes. */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    /** @description `attachment; filename="<topic>-p<partition>-o<offset>.<ext>"` */
                    "Content-Disposition"?: string;
                    [name: string]: unknown;
                };
                content: {
                    "application/octet-stream": string;
                    "application/json": string;
                    "text/plain": string;
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            413: components["responses"]["PayloadTooLarge"];
            502: components["responses"]["BadGateway"];
        };
    };
    sampleMessages: {
        parameters: {
            query?: {
                /** @description Sample size; clamped to 1..25. */
                n?: number;
                /** @description Partition number; -1 = all partitions. */
                partition?: components["parameters"]["PartitionQuery"];
            };
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Sample fetched. */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SampleResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    searchMessages: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody?: {
            content: {
                "application/json": components["schemas"]["SearchRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SearchResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    copyMessages: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
                /** @description Must be `true` when the target cluster is marked `is_prod`; the request is rejected with 428 otherwise. Send only after explicit user confirmation. */
                "X-Kafkito-Confirm-Prod"?: components["parameters"]["ProdConfirmHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                topic: components["parameters"]["Topic"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CopyRequest"];
            };
        };
        responses: {
            /**
             * @description Copy job started; progress is streamed as Server-Sent Events. A
             *     failure *during* the copy also arrives here with status 200 — the
             *     SSE headers are already sent — as a final event carrying `error`.
             *     Clients must inspect the events, not just the status code.
             *
             *     Wire format: one `data: <CopyProgressEvent JSON>` line per event,
             *     separated by a blank line, e.g.
             *     `data: {"copied":500,"skipped":3}`.
             */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "copied": 812,
                     *       "skipped": 5,
                     *       "done": true
                     *     }
                     */
                    "text/event-stream": components["schemas"]["CopyProgressEvent"];
                };
            };
            /** @description Invalid body, missing `dest_topic`, both or neither destination field set, destination equal to the source cluster+topic (the copy would never terminate), unknown `dest_cluster`, `dest_topic` does not exist on the destination (the destination is never auto-created), or `preserve_partition` with too few partitions on the destination topic. */
            400: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            428: components["responses"]["ProdConfirmationRequired"];
            /** @description Too many concurrent copy jobs server-wide (limit 4). The body carries `code: copy_concurrency_limit`. Copies are long-running and each holds broker connections, so the server sheds load rather than queueing. */
            429: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    /** @description Seconds to wait before retrying. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            502: components["responses"]["BadGateway"];
        };
    };
    listGroups: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListGroupsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    createGroup: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["CreateGroupRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ResetOffsetsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            /** @description Group already exists. */
            409: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Error"];
                };
            };
            502: components["responses"]["BadGateway"];
        };
    };
    describeGroup: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                group: components["parameters"]["Group"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["GroupDetail"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    deleteGroup: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                group: components["parameters"]["Group"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeletedNameResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    resetGroupOffsets: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
                /** @description Must be `true` when the target cluster is marked `is_prod`; the request is rejected with 428 otherwise. Send only after explicit user confirmation. */
                "X-Kafkito-Confirm-Prod"?: components["parameters"]["ProdConfirmHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                group: components["parameters"]["Group"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ResetOffsetsRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ResetOffsetsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            428: components["responses"]["ProdConfirmationRequired"];
            502: components["responses"]["BadGateway"];
        };
    };
    listSubjects: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListSubjectsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    deleteSubject: {
        parameters: {
            query?: {
                /** @description `true` performs a hard delete. */
                permanent?: boolean;
            };
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                subject: components["parameters"]["Subject"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteSubjectResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    listSchemaVersions: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                subject: components["parameters"]["Subject"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListSchemaVersionsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    registerSchema: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                subject: components["parameters"]["Subject"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["RegisterSchemaRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["RegisterSchemaResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    getSchemaVersion: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                subject: components["parameters"]["Subject"];
                /** @description Integer version or `latest`. */
                version: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SchemaVersion"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    listAcls: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListACLsResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    createAcl: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ACLEntry"];
            };
        };
        responses: {
            /** @description Created */
            201: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CreateACLResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    deleteAcl: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["ACLEntry"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteACLResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    listScramUsers: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ListSCRAMUsersResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    upsertScramUser: {
        parameters: {
            query?: never;
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
            };
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["UpsertSCRAMUserRequest"];
            };
        };
        responses: {
            /** @description OK */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UpsertSCRAMUserResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
    deleteScramUser: {
        parameters: {
            query?: {
                /** @description Mechanism to delete. Omitted = both mechanisms are tried. */
                mechanism?: "SCRAM-SHA-256" | "SCRAM-SHA-512";
            };
            header?: {
                /** @description Base64-encoded JSON `ClusterConfig` of a private (browser-stored) cluster, max 8 KiB decoded. Honoured when the `{cluster}` path segment is `__private__` (required then). RBAC is bypassed for private clusters. A malformed header is rejected with 400. */
                "X-Kafkito-Cluster"?: components["parameters"]["PrivateClusterHeader"];
            };
            path: {
                /** @description Cluster name from the kafkito config, or `__private__` together with the `X-Kafkito-Cluster` header for a browser-stored cluster. */
                cluster: components["parameters"]["Cluster"];
                user: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Deleted */
            200: {
                headers: {
                    "X-Request-Id": components["headers"]["RequestId"];
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["DeleteSCRAMUserResponse"];
                };
            };
            400: components["responses"]["BadRequest"];
            401: components["responses"]["Unauthorized"];
            403: components["responses"]["Forbidden"];
            404: components["responses"]["NotFound"];
            502: components["responses"]["BadGateway"];
        };
    };
}
